<div>
<% if current_user.following?(user) %>
  <%= button_to "unfollow", unfollow_user_path(id: user.id), method: :delete %>
<% else %>
  <%= button_to "follow", follow_user_path(id: user.id), method: :post %>
<% end %>
</div>