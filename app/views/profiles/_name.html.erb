
<div id="<%= dom_id(user) %>">
<div>
  <%= link_to user.name,
              profile_path(user),
              class: "
                block
                w-fit
                rounded-lg
                bg-(--color-secondary)/50
                hover:bg-(--color-primary)
                font-semibold
                m-2
                p-1",
              data: { turbo_frame: "_top"} %>
              </div>
</div>
