<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Forcebook" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>
  
    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>
    <link rel="shortcut icon" href="app/assets/svg/favicon.svg">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/favicon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;600;700&display=swap" rel="stylesheet">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= javascript_importmap_tags %>
  </head>

<body class="bg-[var(--color-background)] text-[var(--color-text)] h-screen overflow-hidden  md:flex">



    <%= render 'layouts/navbar' %>
    <%= render 'layouts/sidebar' %>
  
    <main class="flex-1 md:ml-5 overflow-y-auto h-screen p-5">
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>    
    <div class="w-full max-w-2xl">
      <%= yield %>
    </div>

    </main>
  </body>
</html>
