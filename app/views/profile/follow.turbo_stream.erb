<turbo-stream action="replace" target="follow_button_<%= @user.id %>">
    <template>
       <%= button_to "Unfollow", unfollow_user_path(@user), 
       method: :delete, 
       form: { data: { turbo_stream: true } } %>
  </template>
</turbo-stream>