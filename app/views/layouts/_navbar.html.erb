<div class="z-0 fixed h-15 w-screen flex flex-row justify-between bg-(--color-background)">
    <div class="font-bold text-2xl m-2 justify-center"> OdinBook </div>
<% if user_signed_in?%>
<div class="flex flex-row">
<!-- /logout btn -->    
    <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete },
                        class: "btn_nav" %>
    
<!-- /logout btn -->
<!-- darkmode toggle -->
      <div data-controller="dark-mode">
          <button 
              data-action="click->dark-mode#toggle"
              data-dark-mode-target="button" 
              class="btn_nav">
            dark mode
          </button>
      </div>
<!-- ... /darkmode toggle ... -->


    <% else %>
    <%= link_to "Sign In", new_user_session_path,
                        class: "btn_nav" %>
</div>
<% end %>

</div>