<div class="flex flex-col space-y-6">


    <div>

        <%= render "posts/form", post: Post.new %>
    </div>

        <%= render "posts/page",
            posts: @posts,
            pagy: @pagy,
            path: ->(opts) { posts_path(opts) } %>
        
</div>